<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ssu.gamertaggo" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        android:name=".utility.AppDefines">
        <activity
            android:name=".viewcontroller.activity.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">

        </activity>

        <activity android:name=".utility.Dispatcher"
            android:theme="@style/AppTheme"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".viewcontroller.activity.LoginActivity"
            android:theme="@style/AppTheme"/>
        <activity android:name=".viewcontroller.activity.SignUpActivity"
            android:theme="@style/AppTheme"/>
        <activity android:name=".viewcontroller.activity.SignUpOrLoginActivity"
            android:theme="@style/AppTheme"/>
        <activity android:name=".viewcontroller.activity.AboutCreatorsActivity"/>
    </application>

</manifest>
